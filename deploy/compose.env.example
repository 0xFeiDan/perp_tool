# Docker Compose infrastructure secrets. Copy to deploy/compose.env on Ubuntu
# and chmod 600 it. Do not put exchange private keys in this file.
POSTGRES_DB=strategy
POSTGRES_USER=strategy
POSTGRES_PASSWORD=
REDIS_PASSWORD=
